- var legacyVows = [ 1, 2, 3, 4, 5 ]
include ../../components/vows/vow-container
include ../page

mixin vowsPage(sheetType)
  if sheetType == 'shared'
    +page("vows", sheetType, 2, true)
      fieldset.repeating_vow
        +repeatingVow()
  else
    +page("vows", sheetType, 2)
      fieldset.repeating_vow
        +repeatingVow()