.sheet-rolltemplate-starforged_oracles
  .sheet-result-row
    padding 0.5em
  .sheet-oracle-dice
    align-self center
    margin-right 0.5em
    span
      border 0px !important
  .sheet-oracle-double-dice
    width 5.7em
    text-align center
    padding 0.1em
    span
      border 0px !important
  .sheet-roll-container
    roll-container()
  .sheet-result-row
    display flex
    justify-content space-between
  .sheet-roll-outcome
    result-row()
  .inlinerollresult
    inline-roll()
  .sheet-subheader
    text-transform uppercase
    sub-base($color_fg, $content-padding)
  .sheet-subfooter
    sub-base($result-bg, $subfooter-padding)
    width 100%
    text-align center
  .sheet-result-body
    padding $content-padding